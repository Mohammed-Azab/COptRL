algo: PPO
model_path: null              # required: path to trained model .zip
vecnorm_path: null            # null → auto-inferred from model_path parent dir

roads:
  - speed_bump
  - flat

n_episodes: 20
deterministic: true
seed: 69

baselines:
  - passive
  - random

results_dir: eval/results
render: false
save_plots: true
